
  # Compact Blog App

  This is a code bundle for Compact Blog App. The original project is available at https://www.figma.com/design/TGEOZTibJbitODiGC25D8l/Compact-Blog-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  